# SMAART Email Assets

Public image assets for SMAART Company internal email campaigns.
Served via the jsDelivr CDN so embedded images never break after an email is sent:

```
https://cdn.jsdelivr.net/gh/SMAART-Company/smaart-email-assets@main/<filename>
```

| File | Used in |
| --- | --- |
| `luxury-cruise-ship.jpg` | Hero image, "Still Ready to SAIL?" reminder email |
| `vote-card-cruise.jpg` | Member Recognition Event card background |
| `champion-roxana.png` / `champion-sondra.png` / `champion-richelle.png` | 2025 Champions row |
| `smaart-logo-white.png` | Header/footer logo fallback |

Keep filenames stable — emails already sent reference them permanently.
